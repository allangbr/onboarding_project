import { Router } from "express";

export const menuItemRoutes = Router();

//Estabelecendo as rotas
menuItemRoutes.post("/create", );

menuItemRoutes.get("/getAll", );

menuItemRoutes.get("/:id", );

menuItemRoutes.put("/:id", );

menuItemRoutes.delete("/:id", );